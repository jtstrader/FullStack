import { GetLatestRetailPricePipe } from './get-latest-retail-price.pipe';

describe('GetLatestRetailPricePipe', () => {
  it('create an instance', () => {
    const pipe = new GetLatestRetailPricePipe();
    expect(pipe).toBeTruthy();
  });
});
