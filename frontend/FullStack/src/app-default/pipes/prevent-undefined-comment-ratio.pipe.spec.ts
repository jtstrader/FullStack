import { PreventUndefinedCommentRatioPipe } from './prevent-undefined-comment-ratio.pipe';

describe('PreventUndefinedCommentRatioPipe', () => {
  it('create an instance', () => {
    const pipe = new PreventUndefinedCommentRatioPipe();
    expect(pipe).toBeTruthy();
  });
});
